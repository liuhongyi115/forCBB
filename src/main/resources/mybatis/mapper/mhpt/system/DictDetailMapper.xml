<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
	namespace="com.taiji.cloudmp.mhpt.modules.system.repository.DictDetailRepository">
	<!-- Result Map -->
	<resultMap id="BaseResultMap"
		type="com.taiji.cloudmp.mhpt.modules.system.domain.DictDetail">
		<result column="id" property="id" />
		<result column="label" property="label" />
		<result column="value" property="value" />
		<result column="sort" property="sort" />
		<result column="dict_id" property="dictId" />
	</resultMap>

<!--	t_cloudmp_dict_detail-->

	<!-- sys_menu table all fields -->
	<sql id="Base_Column_List">
		id,label,value,sort,dict_id
	</sql>

	<sql id="little_Column_List">
		id,label,value
	</sql>

	<!-- 查询条件 -->
	<sql id="Example_Where_Clause">
		where 1=1
		<trim suffixOverrides=",">
			<if test="ids != null">
				and id in
				<foreach collection="ids" index="index" item="item" open="("
					separator="," close=")">
					#{item}
				</foreach>
			</if>
			<if test="label != null and label != ''">
				and label like CONCAT(CONCAT('%', #{label}),'%')
			</if>
		</trim>
	</sql>

	<insert id="save" parameterType="Object">
		<selectKey resultType="java.lang.Long" order="AFTER" keyProperty="id">
			SELECT LAST_INSERT_ID()
		</selectKey>
		insert into
		t_cloudmp_dict_detail(label,value,sort,dict_id)
		values(#{label},#{value},#{sort},#{dictId})
	</insert>

	<update id="update" parameterType="Object">
		update t_cloudmp_dict_detail set
		<trim suffixOverrides=",">
			<if test="label != null  and label != '' ">
				label=#{label},
			</if>
			<if test="dictId != null ">
				dict_id=#{dictId},
			</if>
			<if test="value != null  and value != '' ">
				value=#{value},
			</if>
			<if test="sort != null  and sort != '' ">
				sort=#{sort}
			</if>
		</trim>
		where id=#{id}
	</update>

	<delete id="deleteById" parameterType="Object">
		delete 	 from t_cloudmp_dict_detail where id = #{id}
	</delete>

	<select id="findAll" resultMap="BaseResultMap"
		parameterType="Object">

		select dd.* from t_cloudmp_dict_detail  dd LEFT JOIN t_cloudmp_dict  d on  dd.dict_id=d.id
		<where>
		    1=1
			<if test="dictName != null">
			   and 	 d.`name`  like concat(concat('%', #{dictName}),'%')
			</if>
			<if test="label != null">
			  and 	 dd.`label`  like concat(concat('%', #{label}),'%')
			</if>
		</where>
		order by dd.sort
		<if test="pager.DBPageCondition != null and pager.DBPageCondition != ''">
			${pager.DBPageCondition}
		</if>
	</select>

	<select id="findById" resultMap="BaseResultMap"
		parameterType="Object">
		select
		<include refid="Base_Column_List" />
		FROM t_cloudmp_dict_detail
		where id=#{id}
	</select>

	<delete id="deleteByDictId" parameterType="Object">
		delete 	 from t_cloudmp_dict_detail where dict_id = #{id}
	</delete>

	<select id="getCount" resultType="java.lang.Long"
			parameterType="Object">
		select count(1) from t_cloudmp_dict_detail  dd LEFT JOIN t_cloudmp_dict  d on  dd.dict_id=d.id
		<where>
			1=1
			<if test="dictName != null">
				and 	 d.`name`  like concat(concat('%', #{dictName}),'%')
			</if>
			<if test="label != null">
				and 	 dd.`label`  like concat(concat('%', #{label}),'%')
			</if>
		</where>
	</select>

</mapper>
